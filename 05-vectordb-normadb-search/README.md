# SQL vs Vector Demo

## Docker purge
```bash
docker compose down -v --rmi local --remove-orphans
```

## Docker build
```bash
docker compose build --no-cache
```

## Docker run
```bash
docker compose up
```
